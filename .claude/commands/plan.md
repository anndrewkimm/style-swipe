---
description: Write the next numbered implementation spec for Codex
---
Write an implementation spec for: $ARGUMENTS

1. Look at `plans/` to find the next free number NNN and check specs already DONE/IN_PROGRESS so you don't duplicate or conflict.
2. Read the relevant existing code before speccing — never spec against imagined code.
3. Create `plans/NNN-<slug>.md` from `plans/TEMPLATE.md`: concrete tasks with exact file paths and signatures, 3–7 testable acceptance criteria, and an explicit out-of-scope list.
4. Leave status DRAFT and present the spec's key decisions to the user in a few bullets. If they approve (or asked for it without caveats), set it READY.

If the feature needs a product decision you can't infer (e.g., which retailer feed, UI behavior), ask the user before writing — one batched question, not several rounds.
